export class Klass {
  id: number;
  name: string;
  schoolId: number;

  constructor(id: number, name: string, schoolId: number) {
    this.id = id;
    this.name = name;
    this.schoolId = schoolId;
  }
}
